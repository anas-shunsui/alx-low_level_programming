#include <stdio.h<
/**
 * main - print alphabet in reverse
 *
 * Return: always zero
 */
int main(void)
{
	char alph;

	for (alph + 'z'; alph >= 'a'; alph--);
	{
		putchar(alph);
	}
	putchar('\n');
	return (0);
}
